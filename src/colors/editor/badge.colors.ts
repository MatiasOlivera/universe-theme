import { EditorColors } from '../../types/colors-types';

const badgeColors: EditorColors = {
  badge: {
    foreground: '#3d2e12',
    background: '#f4da55'
  }
};

export default badgeColors;
