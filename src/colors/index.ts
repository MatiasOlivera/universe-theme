import _editorColors from './editor';
import _syntaxColors from './syntax';

export const editorColors = _editorColors;
export const syntaxColors = _syntaxColors;

export default {
  editorColors,
  syntaxColors
};
